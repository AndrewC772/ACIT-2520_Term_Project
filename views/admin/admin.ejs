<div>
    <h1>Dashboard</h1>
    <p>Welcome <%= user.name %></p></br>
    <p>Current Active Sessions:</p>
    <% for (const session in sessions) { %>
        <p>SessionID: <%= session %> </p>
        <%  let session_object = sessions[session] %>
        <p>UserID: <%= session_object["passport"].user %></p>
        <form>
            <input  name="SessionID" class="form-control"  formmethod="post">
            <button type="SessionID" class="btn btn-primary" formmethod="post">Revoke Session</button>
        </form>
    <% } %>
</div>